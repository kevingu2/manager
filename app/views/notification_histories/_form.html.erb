<%= form_for(@notification_history) do |f| %>
  <% if @notification_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notification_history.errors.count, "error") %> prohibited this notification_history from being saved:</h2>

      <ul>
      <% @notification_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
