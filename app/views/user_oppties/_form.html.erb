<%= form_for(@user_oppty) do |f| %>
  <% if @user_oppty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_oppty.errors.count, "error") %> prohibited this user_oppty from being saved:</h2>

      <ul>
      <% @user_oppty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :oppty_id %><br>
    <%= f.text_field :oppty_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
