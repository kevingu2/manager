<h1>History</h1>

<table>
  <thead>
    <tr>
      <th>Opptyname</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @histories.each do |history| %>
      <tr>
        <td></td><%= link_to history.opptyName, oppties_path(oppty_id:history.oppty) %></td>
        <td><%= link_to 'Show', history %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New History', new_history_path %>
